import { test, expect } from '@playwright/test'; import { login } from './helpers/auth';
test.describe('mastery and revision @regression',()=>{test.beforeEach(async({page})=>login(page));test('mastery state is visible',async({page})=>{await page.goto('/problems/longest-substring-without-repeating-characters');await expect(page.locator('body')).toContainText(/mastery|learning|practice|mastered/i);});test('revision route is reachable',async({page})=>{await page.goto('/revision');await expect(page.locator('body')).not.toContainText(/404|not found/i);});test('progress route is reachable',async({page})=>{await page.goto('/progress');await expect(page.locator('body')).not.toContainText(/404|not found/i);});});
