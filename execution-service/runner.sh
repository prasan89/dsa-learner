#!/bin/sh
# Minimal HTTP execution service — receives Java code + test input, compiles, runs, returns output.
# In Phase 1 Week 3 this will be replaced with a proper Spring Boot microservice.
# For now it documents the contract: POST /execute → { stdout, stderr, exitCode, runtimeMs }
echo "Execution service stub — replace with Spring Boot service in Week 3"
